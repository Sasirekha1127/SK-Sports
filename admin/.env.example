# Set to "false" to point the admin at a real backend instead of the
# built-in browser-storage mock data layer. See src/services/httpClient.js
VITE_USE_MOCK=true
VITE_API_URL=http://localhost:4000/api
